fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
websockets>=13.0
sqlalchemy>=2.0
asyncpg>=0.30
psycopg2-binary>=2.9
pydantic>=2.0
pydantic-settings>=2.0
google-genai>=1.0
httpx>=0.27
python-dotenv>=1.0
faster-whisper>=1.0
# Test-only: imported directly by test_qa_live.py, test_e2e_live.py and
# test_groq_live.py to decode audio fixtures. The running server never needs
# them, but without them those scripts die at import time.
numpy>=1.26
soundfile>=0.13

